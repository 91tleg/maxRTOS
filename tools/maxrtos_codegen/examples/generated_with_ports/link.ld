/*
 * AUTO-GENERATED by maxrtos_codegen -- DO NOT EDIT BY HAND.
 * Source: examples/example_module_with_ports.json
 *
 * This file lays out kernel memory and one NOLOAD section per partition
 * stack. Board-specific sections (isr vector table contents, heap/stack
 * sizes for the main stack, etc.) belong in the board's own linker
 * fragment.
 */

ENTRY(Reset_Handler)

MEMORY
{
    FLASH      (rx) : ORIGIN = 0x08000000, LENGTH = 2097152
    DTCM       (xrw) : ORIGIN = 0x20000000, LENGTH = 131072
    SRAM       (xrw) : ORIGIN = 0x24000000, LENGTH = 524288
}

SECTIONS
{
    .isr_vector :
    {
        . = ALIGN(4);
        __isr_vector_start = .;
        KEEP(*(.isr_vector))
        . = ALIGN(4);
    } >FLASH

    .text :
    {
        . = ALIGN(4);
        *(.text)
        *(.text*)
        . = ALIGN(4);
        _etext = .;
    } >FLASH

    .rodata :
    {
        . = ALIGN(4);
        *(.rodata)
        *(.rodata*)
        . = ALIGN(4);
    } >FLASH

    _sidata = LOADADDR(.data);
    .data :
    {
        . = ALIGN(4);
        _sdata = .;
        *(.data)
        *(.data*)
        . = ALIGN(4);
        _edata = .;
    } >DTCM AT>FLASH

    .bss :
    {
        . = ALIGN(4);
        _sbss = .;
        __bss_start__ = _sbss;
        *(.bss)
        *(.bss*)
        *(COMMON)
        . = ALIGN(4);
        _ebss = .;
        __bss_end__ = _ebss;
    } >DTCM

    ._user_heap_stack :
    {
        . = ALIGN(8);
        PROVIDE(end = .);
        PROVIDE(_end = .);
        . = . + _Min_Heap_Size;
        . = . + _Min_Stack_Size;
        . = ALIGN(8);
    } >DTCM

    /* Partition stacks packed into SRAM, in partition-id order. */
    .partition_control_stack (NOLOAD) :
    {
        . = ALIGN(512);
        __partition_control_stack_start = .;
        *(.partition_control_stack)
        . = ALIGN(512);
    } >SRAM
    .partition_application_stack (NOLOAD) :
    {
        . = ALIGN(512);
        __partition_application_stack_start = .;
        *(.partition_application_stack)
        . = ALIGN(512);
    } >SRAM
    /* Static IPC port buffers packed into SRAM, in
     * declaration order. Each is aligned to its own size because the
     * MPU requires a region's base address be a multiple of its size. */
    .port_cmd_channel (NOLOAD) :
    {
        . = ALIGN(128);
        __port_cmd_channel_start = .;
        *(.port_cmd_channel)
        . = ALIGN(128);
    } >SRAM
    .ARM.attributes 0 : { *(.ARM.attributes) }
}

/* Main/interrupt stack: top of the kernel memory region. */
__StackTop = ORIGIN(DTCM) + LENGTH(DTCM);
_Min_Heap_Size = 0x200;
_Min_Stack_Size = 0x400;